#include <bits/stdc++.h>

using namespace std;

int main(){

    return 0;
}

int Insere(aponta * raiz, item item){
    if ((*no)->reg == NULL){
        tipo no = (aponta_no)malloc(item);
        (*no)->dir = null;
        (*no)->esq = NUll;
        (*no)->reg = item;
    }
    if ((*no)->reg > item){
        Insere(&(*no)->esq)
    }
    insere(&(*no)->dir);
    return 0;
}